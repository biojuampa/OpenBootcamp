let altura_cm = 185;
let altura_mts = 1.85;
let peso = 80.9;

let altura_roundUp = Math.ceil(altura_mts);
let peso_roundDown = Math.floor(peso);

let maximos = Number.MAX_VALUE === Number.MAX_VALUE + 1;

console.log(altura_cm);
console.log(altura_mts);
console.log(peso);

console.log(altura_roundUp);
console.log(peso_roundDown);

console.log(maximos);
